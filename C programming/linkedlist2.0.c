#include<stdio.h>
#include<conio.h>
#include<stdlib.h>
struct node
{

    intdata;
    struct node *next;
    struct node *prev;
};
main;
{

    struct node *start=NULL;
    struct node  *last,*temp,*new node;
    int,i,n,data;
    printf("\n enter the elements\n")
    scanf("%d",&n);
    printf("\n read the elements);
    for(i=1,i<=n,i++)
    {

        scanf("%d",&data);
        struct node * new node=struct
    }

}

    };
};
